# What the markers mean

Files in this repository sometimes say that something is *"tracked as `dn8`"* or
*"tracked separately (`dn4-39`)"*. This page says what those markers are, so that a
marker you are pointed at is one you can look up.

It exists because of a rule this repository broke once and now states out loud, in
[`CONTRIBUTING.md`](../CONTRIBUTING.md) § *Conventions in this repository*:
**nothing that is pushed lies**, and anything that cannot be fixed before a push is
written down *with the marker that will fix it*. A marker the reader cannot resolve
is only half of that promise.

## Where the markers come from — and what you cannot see

DeskNode is planned in a separate, **unpublished** working repository. Each marker is
a key in that plan: `dnE-N` is story *N* of epic *E*, and `dnE` on its own is a whole
epic. **The plan itself is not part of this clone and there is no schedule in it that
would be worth publishing** — what follows is the meaning of each marker and its
state, not a commitment to a date.

⚠️ **This page is not the changelog and not the history.** It lists only the markers
that name work **still to come**. Markers that name *past* work — most of `dn1-*`,
`dn2-*`, `dn3-*` and `dn4-*`, which appear throughout [`README.md`](../README.md) and
[`dn4-15-arbitrage.md`](dn4-15-arbitrage.md) — are described where they are cited;
they are the engineering log, and this page does not duplicate it.

⚠️ **Four rows restored on 2026-09-04, at the code review of `dn5-2` — and the reason is
the rule just below, not a change of mind.** `dn5-2` deleted **its own row** the day it
closed, under the older rule that this page lists only work still to come; `dn5`, `dn5-1`
and `dn5-3` had never been given one. All four are **cited in this tree** — in
[`CONTRIBUTING.md`](../CONTRIBUTING.md), [`LICENSING.md`](../LICENSING.md),
[`README.md`](../README.md) and [`dn5-1-ecart-promesses.md`](dn5-1-ecart-promesses.md) —
so under the admission rule below they belong here. ⛔ Measured, not assumed: **30**
markers are cited in the prose without a row, and the other **26** are `dn1-*`..`dn4-*`,
which the paragraph above deliberately excludes as the engineering log.

⚠️ **Amended on 2026-09-04, and the sentence above is kept rather than replaced.** A
marker that is *cited in this tree* stays listed here **after it closes**, with the state
`done`, instead of vanishing: a marker you are pointed at must remain one you can look
up, and that promise does not expire the day the work does. What the paragraph above
still rules out is unchanged — a marker nobody cites here does not get a row just because
it exists. ⇒ the admission rule is **"cited in this tree"**, ⛔ not "not finished yet".

⚠️ **Sharpened on 2026-09-05 (`dn5-5`), because the rule as written was ambiguous and the
ambiguity had a live case.** *"Cited in this tree"* means **cited in prose a reader is
pointed by** — the pages at the root and under `docs/`. It does **not** mean *"the string
appears somewhere in the tree"*: `mesures/` is a raw record of tool output, and a marker
named inside a capture is a fact about a measurement, ⛔ not a pointer offered to a reader.
The case that forced the distinction: `dn5-5` gave `dn6` a row because it referred a reader
to this page for it, and gave **`dn5-6` none**, although `dn5-6` is named in
`mesures/dn5-5/T5-inventaire-reste-a-faire.txt`. That asymmetry is deliberate, and this
paragraph is why. ⇒ **The day any page here points a reader at `dn5-6`, it gets a row** —
and `tools/verif_licences_dn52.py` will say so, since it anchors on exactly that: a marker
sent to this page.

⚠️ **Widened on 2026-09-05 (`dn5-6`, finding 9), and the sentence above is kept rather
than replaced.** *"The pages at the root and under `docs/`"* was the scope of the check,
not the scope of the rule — and the two had drifted apart. A workflow file under
`.github/workflows/` is prose a reader is pointed by just as much as a page is: it
carries markers, owners and hand-offs. Measured the same day: the check read `.md` only,
so a marker named in `gates.yml` was **never** examined, while `gates.yml` itself claimed
the check covered every marker cited anywhere. ⇒ the check now reads those workflow files
too, and the rule and the check say the same thing again. ⚠️ What did **not** change is
the anchoring: only a marker *sent to this page* is examined, wherever it is written —
`mesures/` is still a raw record, not a pointer offered to a reader.

🔴 **And the case that had motivated all this was already gone.** The finding was raised
on *"`gates.yml` points at `dn4-45`, which the roadmap does not define"*. By 2026-09-05
every marker cited under `.github/` was defined here, because `dn5-5` had added the
`dn4-45` row. Widening the check therefore leaves it **green**: ⛔ no red can show this gap.
🔴 **The count is deliberately not written down here.** The first draft of this paragraph
said *"all **four** markers"* — and the very commit that wrote it added two more
(`dn5-5`, `dn5-6`, now cited in `gates.yml`). A count written into prose is stale the
moment the prose changes, which is the fifth time this epic has paid for that. Take the
reading yourself: `git grep -h -o -E 'dn[0-9]+-[0-9]+' -- .github | sort -u`. What shows it is the mutant that
replants the fault (`verif_licences_dn52.py --mutant 34`), and that is why it exists.

🔴 **How to read the `state` column — this is a rule, ⛔ not a promise of freshness.** The
authority on any state is the unpublished plan, ⛔ never this page: this column is a
**snapshot, refreshed when the work that changes a marker also passes through here**. It
was wrong three times at once on 2026-09-04 (see the rows below), which is why the rule is
now written instead of assumed. ⛔ If a row disagrees with something else in this tree,
the row is the one to doubt.

## The markers cited in this tree

| marker | what it covers | state |
|---|---|---|
| `dn4-5` | Long-running soak of the agent module on its own. | in progress |
| `dn4-39` | Making the repository's gates run **without anyone having to think about it**. ✅ Done on 2026-09-02: `.github/workflows/gates.yml` runs `bash tools/run_gates.sh` on every push and every pull request, and the six gates that cannot be exercised on a runner are **declared** with their reason, a witness path and an exact expected exit code — ⛔ not silenced. ⚠️ One piece of it is **not** covered and it is not a CI matter: the generated table in the private planning repository is regenerated **at the workstation**, because that repository is never cloned — that part is carried by `dn5-5`, ⛔ it does not keep this marker open. *(Reworded on 2026-09-04 at the code review: it read "⚠️ **Still open**, …", which left the cell saying `done` and "still open" at the same time — the very contradiction the correction beside it had just claimed to remove. The earlier wording is named here rather than erased.)* ⚠️ **State corrected on 2026-09-04 (`dn5-4`): this row read `in review`, and the marker had been closed on 2026-09-02.** The sentence above already said "Done on 2026-09-02", so this page contradicted itself in a single cell. ⛔ Note the earlier wording was *"`tools/run_gates.sh` exists and works; nothing invokes it automatically."* — the **first half stays true** (the script exists and works, and it is still the manual command); it is the **second half** that changed. *(Restored at the code review of 2026-09-02: the first clause had been dropped rather than annotated, which §14 of the story forbids.)* | done |
| `dn4-40` | Stopping gates from passing on nothing, and anchoring their manifests **by pattern instead of by line number**. ⚠️ **State corrected on 2026-09-04 (`dn5-4`): this row read `not started`, and the marker had in fact been closed.** ⛔ The earlier wording is kept above rather than replaced. One acceptance criterion did not close with it and was handed on rather than dropped — it is carried by `dn4-44`, whose own work armed it. | done |
| `dn4-41` | The board on its own is enough — the two hardware tiers behave correctly without the ambient sensors. | in progress |
| `dn4-42` | The display speaks two languages. | in review |
| `dn6` | The hardware side: a bill of materials for both tiers, the wiring diagram and the I²C addresses, verified supplier links, and the 3D-printed case — **and making the wiring photographs lighter**. 🆕 **Row added on 2026-09-05 by `dn5-5`, under the admission rule above**: that work is the first to cite this marker in this tree, and a marker you are pointed at must be one you can look up. It is also the marker that carries the largest single thing in this repository: measured on **`76b031a`** — the commit is named because "at `HEAD`" means a different tree every day — `docs/` weighs **27 992 816 bytes** of the **46 254 844** held by the **603** tracked files, almost all of it photographs. *(This row first said "27.99 MB of the 46.14 MB at `HEAD`"; the total was the **parent** tree's and the row carried no anchor at all. The wording is corrected rather than the figure quietly swapped.)* ⛔ Lightening them is **not** done by rewriting history — see [`CONTRIBUTING.md`](../CONTRIBUTING.md) § *Conventions in this repository*.  ⚠️ **State corrected on 2026-09-07, by `dn6-4` itself: this row read `not started` while three of the epic's four pieces had already shipped** — the bill of materials, the wiring page and the photograph curation all closed between 2026-09-06 and 2026-09-07. ⛔ The earlier value is named here rather than erased. 🔴 **And the 3D-printed case named above is deferred past the first public release** (decision of 2026-09-06). What shipped on 2026-09-07 is **not** the case: it is the *place* it will live in, the file formats accepted there, the licence that already covered it, and the printing notice — whose four parameters all come out **not measured**, because nothing has been printed and there is nothing to print. Read [`boitier.md`](boitier.md); the plans folder is `docs/boitier/` and it holds **no plan**. The drawing itself belongs to the owner, ⛔ and moving *when* it happens does not move *who* does it. | in progress |
| `dn6-4` | **Where the 3D-printed case will live, in what formats, under which licence, and what is still unknown about printing it.** Shipped on 2026-09-07 with its gap written down rather than filled: the folder `docs/boitier/` exists, the six accepted file formats are named, the licence was already granted by the `docs/` row of [`LICENSING.md`](../LICENSING.md) — which names *enclosure plans* and was **not** touched — and the printing notice carries **four** parameters, every one of them **not measured** with the reason and the owner written beside it. 🔴 **No plan is deposited**, and `tools/verif_boitier_dn64.py` keeps that statement true in both directions: while the folder is empty the page must say so, and the day a plan lands the sentence becomes false and the check goes red. ⛔ This row is not a promise that the case will be drawn: the drawing is the owner's, and it comes after the first public release. ⚠️ Following the rule stated for `dn5-4`, what is written is the state the marker has **in the commit that publishes this line**: its code review has not run yet, so it lands here as `in review`, ⛔ not `done`. ⚠️ **Corrected within the same day, by the review this sentence anticipated.** It ran: thirty-five findings, twelve fixed — one of them a check that could never go red, because `git ls-files` escapes non-ASCII paths, so an accented plan file was invisible to the two-way promise above. Following the rule stated for `dn5-4`, what is written is the state the marker has **in the commit that publishes this line**, and that state is now `done`. ⛔ The earlier value is named here rather than erased. ⚠️ One finding was deferred rather than fixed, and it is about this page: **no check confronts the state column of any row here with the plan** — a row can say one thing while the marker says another, exactly as the `dn6` row above did, and every check stays green. | done |
| `dn6-5` | **Whether this repository carries affiliate links, which programmes actually exist for the suppliers it names, on what terms, what French and EU law requires to be disclosed, and what has to become true before the answer changes.** Shipped on 2026-09-07 as [`affiliation.md`](affiliation.md), with the state written rather than promised: **no affiliate link is carried**, measured over every tracked `.md` and `.txt` file — 436 of the 685 files git tracks, with the 97 tracked text files outside that sweep re-read by hand once, also at zero. The survey **derives** its population from the purchasing page instead of listing it, so it comes out at **two** suppliers of record out of the eleven domains the page names — the other ten being the addresses `dn6-1` tried and failed to reach, and `waveshare.com` belongs to **both** lists at once, because it is the manufacturer as well as one of the addresses that had failed; the page states that overlap rather than presenting a clean split — and the day the bill of materials names a new `Source` domain, the survey goes red instead of quietly rotting. 🔴 **Nothing was signed**: no account was opened, no terms were accepted, no tracking identifier was written, and the page says so — signing up is the owner's gesture, and every gesture it lists carries its owner. ⚠️ What that costs is written too: AliExpress's admission terms sit **behind a login**, so four conditions (cookie window, payout threshold, per-order cap, approval criterion) come out **not read** rather than reconstructed. 🔴 **The survey corrected itself before this line was published, and that correction is its most useful finding**: two rows first recorded that Waveshare answered **HTTP 403** "to any automated retrieval". It does not — with an ordinary browser header the same two addresses answer **200**, and the programme states its terms outright: 2% base commission, tracking valid at least 72 hours, US$5 PayPal payout threshold, and a *Who can Join* section that names **GitHub** among the platforms it invites, while this repository is private there. ⇒ *"not reached"* is a property of the **retrieval method**, ⛔ not of the address; a failure verdict has to say what it was tried with. ⛔ The refuted sentences are **described** beside their correction rather than erased — described and ⛔ not quoted, because the gate counts failure verdicts in that page and quoting the old one verbatim would add a fresh occurrence of the very defect the correction removes, and the same claim standing in [`bom.md`](bom.md) about the product page is **not** patched here — it is recorded with its owner instead, because a price table is re-surveyed, not spot-fixed. `tools/verif_affiliation_dn65.py` keeps the statement true **in both directions**: while no published address carries an affiliate marker both pages must say so, and the day one does, the sentence becomes false and the check goes red — including when nobody remembered to update the declaration. ⛔ No revenue milestone appears anywhere: those were taken out of scope by the owner on 2026-09-06, and the switch-over point deliberately reintroduces none. ⚠️ Following the rule stated for `dn5-4`, what is written is the state the marker has in the commit that publishes this line. **The first version of this line said the code review had not run; it has, on 2026-09-08, and the sentence is corrected rather than erased.** It raised 43 findings across four independent readings, of which the two heaviest were about the checker rather than the page: the guard that is supposed to declare a stale mutant refused to fire for five of them, and the branch carrying the whole marker arbitration was aimed at by no mutant at all. Both are fixed and both now carry a mutant that replants the fault. ⚠️ **One question is deliberately left open**: whether this work belongs to the `dn6` epic or to the shop-window epic. Both arguments are real and both are recorded in the planning ledger; the arbitration is the owner's, ⛔ not this marker's. | done |
| `dn7` | The install side: the browser flasher, a packaged agent you do not have to run from source, and **the language chosen at install time** rather than on the panel. ⚠️ **State corrected on 2026-09-08, by the first story of the epic itself: this row read `not started`, and the epic had started.** ⛔ The earlier value is named here rather than erased. What has shipped is the local installer and nothing else — ⛔ no flashing, ⛔ no language choice; see the `dn7-1` row below for what that is exactly. ⚠️ **Amended on 2026-09-09, by the epic's own third story: the sentence above is kept rather than replaced, and both of its ⛔ clauses have since stopped being true.** It read *"no flashing, no language choice"*; the page **flashes** the board since 2026-09-08 (`dn7-2`), and it **carries the panel's language choice** since 2026-09-09 (`dn7-3`) — offered above the flash button, English by default, and written into the board by a separate gesture. ⇒ what is left open in this epic is a packaged agent you do not have to run from source, which is the remaining clause of the sentence that opens this row. | in progress |
| `dn7-1` | **The installer starts without elevation, and the page already drives the agent.** Shipped on 2026-09-08 as `installeur/`: one file to double-click, which serves a page from the machine itself on `http://127.0.0.1:<port drawn at launch>` — ⛔ never hosted, ⛔ never opened from disk, and ⛔ never on a fixed port, because *updating DeskNode means reinstalling it*, so the second launch is everybody's normal case rather than an edge one. The page **exposes** two verbs the repository already had — `stop` and `retirer` of `tools/dn_agent_tour.ps1` — and ⛔ reimplements neither; after `retirer` the scheduled task is **asked for again**, because the tool's own exit message is not proof. 🔴 **Nothing here asks for administrator rights**, and that is mechanical rather than declared: the task stays at `RunLevel=Limited` and the guard that treats anything else as a defect never fires. ⚠️ **One requirement closes as a written gap, ⛔ not as a tick**: without a standalone executable the newcomer installs Python 3 plus `psutil` and `pyserial`, the README says so, and the **owner of the return to a single download is the standalone agent, planned for V0.2**. ⚠️ **And one half of a measurement could not be taken**: the download friction was reproduced faithfully with the Mark of the Web, but a *real download from a real release* could not be — there is none, and that half is carried by `dn8`. ⛔ Not flashing, ⛔ not the language choice, ⛔ no render preview — the last of those is a **declared exclusion** of the first version. Following the rule stated for `dn5-4`, what is written is the state the marker has **in the commit that publishes this line**: its code review has not run yet, so it lands here as `in review`, ⛔ not `done`. ⚠️ **Corrected within the same day, and the sentence above is kept rather than replaced: the review has run.** It raised twenty-six findings, all of them acted on, and two were **demonstrated** rather than argued — the check that was supposed to prove the removal only looked for the *presence* of a re-query, so swapping two branches made the page say the task was gone while it survived, and the exit codes the README publishes as "the mechanical instrument of the gap" were played by nothing at all. Both now run the shipped code against a paper double. 🔴 **The review also paid for its own trap**: after demonstrating a fault and restoring the file, the check kept failing on a sound source, because the ordinary import path reuses `__pycache__`, validated on size and timestamp — a check that measures yesterday's code is worse than no check, and the source is now read and compiled instead. The state stays `in review`: the review produced fixes, it did ⛔ not close the marker. ⚠️ **State corrected on 2026-09-11, by the epic's retrospective: the sentence above is kept rather than replaced (`NFR3`), and it stopped being true on 2026-09-08.** The review DID close the marker that day — `3d637b6`, *« La marche passe `review` => `done` »* — and the tracker has read `done` ever since. This row was not moved with it, and said `in review` for three days across three commits that touched this very file. | done |
| `dn7-2` | **The board flashes from the page, what blocks it is understandable, and where the firmware comes from is written down.** Shipped on 2026-09-08: `installeur/charge/` carries the **four images and their manifest** — bootloader, partition table, application and the asset — written at `0` / `32768` / `65536` / `4259840`, `chipFamily` **ESP32-S3**, ⛔ with **no merged binary**, by ESP Web Tools **pinned to `10.4.0`** (the range form `@10` answered a 302 on 2026-09-08 and would have moved on its own). 🔴 **The announced version is READ out of the binary, ⛔ never copied beside it**: it is `esp_app_desc_t.version`, and `tools/verif_flash_dn72.py` refuses any difference between it and the manifest — the throwaway prototype of 2026-08-31 announced `0.1.0-beta-essai`, its own test-bench value, over images nobody could date. **Updating means reinstalling**, so the second launch is everybody's normal case and the page **frees the serial port itself**: it plays the existing `stop` verb **on the port it discovered** (`VID_303A&PID_1001`, interface `MI_00`) instead of the hard-coded `COM3` default — and it reads that verb's exit codes as **facts rather than a boolean**, because `7` means *the agent stopped but the port is gone*, which is neither a success nor a failure. Four named obstacles get the page's own words: the flashing tool's fallback dialog offering **CP2102 / CH340 / CH342 drivers that this native-USB board does not need**, the **real name of the port** in both the forms Windows and the chip give it — ⛔ it is not called « DeskNode » — the **two gestures** the browser's port picker needs, and the **recovery procedure**. ⚠️ **Two gaps are written rather than ticked.** The provenance of those four images is **mechanised by nothing**: no CI builds this firmware (`dn4-45`, `not started`), they were produced by hand, and `installeur/charge/PROVENANCE.md` says so with the exact revision, the command and the four hashes. And the **GPL obligation to point at the source of this exact revision** is met by the revision read out of the binary, ⛔ not by a publicly reachable source: measured on 2026-09-08, `git tag -l` is **empty**, `gh release list` is **empty**, and this repository is private — that residue is carried by `dn8`. ⚠️ The recovery wording is **what was measured, ⛔ not what was assumed**: the only procedure written in this repository is **BOOT held + RESET**; « BOOT held while plugging in » appears nowhere, and the page says the true one. Following the rule stated for `dn5-4`, what is written is the state the marker has **in the commit that publishes this line**: its code review has not run yet, so it lands here as `in review`, ⛔ not `done`.  ⚠️ **State corrected on 2026-09-09, by `dn7-2-2`: this row read `in review`, and the review had run and closed the marker on 2026-09-09.** ⛔ The earlier value is named here rather than erased. Following the rule stated for `dn5-4`, what is written is the state the marker has **in the commit that publishes this line**, and that state is `done`. ⚠️ **And one of this row's own claims was refuted after it was published**: the sentence above says the page gives the port's real name *in both the forms Windows and the chip give it*. The browser's picker was opened on 2026-09-09 and shows **one** line — the chip's product string — with a button reading « Connexion ». ⛔ The marker is **not reopened** and its acceptance criterion is **not unticked**: it is annotated as refuted, and the gap is carried by `dn7-2-2` below. | done |
| `dn7-2-2` | **The page says the port's real name, and you can tell what to do on it.** Shipped on 2026-09-09, and ⛔ **not** a reopening of `dn7-2`, which stays `done`: its flash works and was taken to completion twice on 2026-09-09. What was wrong is what the page **said** and what you could **do** on it. 🔴 **The browser's port picker was finally opened, and it refutes what the previous story published**: it shows **ONE line**, `USB JTAG/serial debug unit (COM3)`, with a button reading **« Connexion »** — while the page announced *the two forms* and *Se connecter*. Edge names the port by the chip's **USB product string**, ⛔ not by the localised Windows `FriendlyName`; the earlier measurement was taken in the **Device Manager** and generalised to a picker nobody had opened. ⚠️ The server-side discovery was **never wrong** — it targets interface `MI_00` and returns the right `COM<n>` — so ⛔ nothing was "fixed" there: it was the **display** that lied. 🔴 **The two checks that guarded the old wording were made to go RED before going green again**, against the original page: a check that goes green without ever having gone red guarded nothing. Four more mutants close three constants that were guarded by none. The page also **merges** the two buttons that posted the same `stop` verb on the same port and returned the same output — naming them by what set them apart was impossible, because nothing did — gains a **separate** entry that opens its own serial port at **115200** baud so a board can be read **without being reflashed**, and speaks **English and French**, English by default, with both languages living in the same document as sibling elements so the default is **structural** rather than promised. ⚠️ ⛔ **That is the language of the page, not of the panel**: two different languages, and the panel's own choice is a different marker. A new gate, `tools/verif_page_dn722.py` (56 checks, 32 mutants), keeps all of it. ⚠️ **One acceptance criterion is deliberately left OPEN, with its instrument named**: *the next gesture is identifiable without unfolding anything* is measured **by the owner's eye**, ⛔ by no character count — this pass ships the change, writes what it did, and carries the criterion open in the planning ledger, with an owner observation session as its carrier. ~~Following the rule stated for `dn5-4`, what is written is the state the marker has **in the commit that publishes this line**: its code review has not run yet, so it lands here as `in review`, ⛔ not `done`.~~ ⚠️ **State corrected within the same day, by that review itself: this row read `in review`, and the four-layer review then ran and closed on 2026-09-09.** ⛔ The earlier value is named here rather than erased, and the sentence above is struck rather than replaced. It found **57 items — 43 patched, 3 carried, 11 rejected with reasons** — and its **nine `high` were one family**: a gate that stayed **GREEN on code that was FALSE**, each one **replayed** rather than argued. Among them, the check that claimed the console entry was *distinct* from the flash button compared the offsets of two different substrings, so it could never fail; and the English text told an English reader to click « Connexion », a label read **once**, on a **French-language** Edge — the very generalisation from `n = 1` this marker exists to refute. Both are fixed, and the counts above are the ones measured after those fixes. Following the same `dn5-4` rule, the state written is the one the marker has in the commit that publishes this line, and that state is `done`. | done |
| `dn7-3` | **The panel's language is chosen at install time, and a separate gesture writes it into the board.** Shipped on 2026-09-09: the install page offers **two positions**, **English by default**, **above the flash button** and visible without unfolding anything, plus a **separate, explicit gesture** that writes the choice into the board after the flash. 🔴 **⛔ THIS ROW DOES NOT CLAIM THE BOARD HAS THE LANGUAGE ON ITS FIRST BOOT, AND THE REASON IS MEASURED**: flashing **wipes the board's stored settings** (measured on 2026-09-09 — after a flash from this page, the board's `cfg` command answers *no config in NVS, defaults applied*), and the board **restarts on its own before** the gesture is played. ⇒ **the first boot after an install is ENGLISH whatever was chosen**, the panel switches when the gesture is played, and the gesture is **to be replayed after every install** — updating DeskNode means reflashing it. The page says so at the step that sets the language; this row says it too rather than leaving it to be discovered on a panel that came back English. ⛔ **Nothing was added to the firmware** — the console command, the NVS key and the one-definition-per-string table were already shipped — and `git diff -- firmware/` is empty for this marker: the page adds **the choice**, ⛔ not the mechanism. 🔴 **Choosing English writes nothing at all, and that is the property rather than a shortcut**: the board starts in English when its stored settings carry **no** language, so writing a value meaning *English* would replace a **default** with a **choice**. The page ⛔ does not promise more than it knows either: it has **not read** what the board carries, so it ⛔ does not claim an English start on a board whose settings already hold French. 🔴 **The path was decided on measurement, ⛔ not at framing**: the serial console after the flash, ⛔ not a pre-baked settings partition — and the deciding reason is that a pre-baked partition **cannot relay a refusal**, which is exactly what this marker requires. 🔴 **The gesture is explicit rather than chained to the end of the flash**, and two measurements decide that: the page holds **no listener** on the flashing module, and **the delay between the end of the flash and the moment the board's prompt answers is not measured** — sending too early loses the line, and a lost line would produce *a success announced on a language that was never set*. The gesture therefore wakes the console and **waits for the prompt** before sending anything. 🔴 **The acceptance is read, then the state is read back — ⛔ never the order that was sent**: *set* is reachable only from a re-read that carries the chosen code. There are **five distinct outcomes**, each recognised by the literal the `langue` command prints **for it** — *set* · *refused* · *applied but ⛔ NOT stored* · *no answer* · *no prompt* — with the board's own words relayed verbatim. ⚠️ The fifth exists because it was **measured**: when the board fails to store the setting it prints its warning, **then rebuilds the scene, then prints the success line**, and returns non-zero — one key saying *the panel HAS switched and it will ⛔ NOT keep it*, because *refused* and *set* are both false there. ⚠️ And **`set` ⛔ does not promise a reboot survives it**: the board applies the setting **before** storing it, so the re-read answers the same either way — the only proof is the panel's own top bar after a power cycle. **Two** gates keep all of it. `tools/verif_langue_dalle_dn73.py` (61 checks, 34 mutants) reads the structure, **re-reads inside `firmware/`** the six answer literals it anchors, and pairs them **both ways** with the patterns the page matches — including a check that no classification pattern can match an **unrelated** log line, because the board's console carries the word *REFUS* for the I²C bus and the clock too, on the same stream, in the cold-start window where this gesture is played. And `tools/verif_banc_langue_dn73.py` (40 checks, 28 mutants) **runs** `tools/banc_langue_dalle_dn73.mjs`, which **extracts the page's script and executes it** against a stub port and replays the **nine** rows of the input/output matrix plus **nine** more paths — the first thing in this repository ever to execute that JavaScript, and the answer to a gap this repository had carried openly since `dn7-2`. ⚠️ `node` is a **declared prerequisite**: without it that gate returns 4 with its reason and is declared non-playable, ⛔ never green on a bench that did not run. ⚠️ **Two acceptance criteria are deliberately left OPEN, with their instrument named**: that the language is *really* on the board and *survives a power cycle* is read **on the panel's own top bar, by eye**, and that the refusal is *seen on screen* needs a real board told a code it does not carry. Both are carried in the planning ledger, with a board session as their carrier. Following the rule stated for `dn5-4`, what is written is the state the marker has **in the commit that publishes this line**: its code review has not run yet, so it lands here as `in review`, ⛔ not `done`.  ⚠️ **State corrected within the same day, by the review this sentence anticipated: this row read `in review`, and the review has since run and closed the marker on 2026-09-09.** ⛔ The earlier value is named here rather than erased. It ran **twice**: the first pass found the page announcing the opposite of what the board prints when a setting is applied but cannot be stored — the whole implementation was reverted and re-derived — and the second found four visible properties guarded by nothing, each **demonstrated** by mutating a copy while every check stayed green. Following the rule stated for `dn5-4`, the state written is the one the marker has in the commit that publishes this line, and that state is `done`. | done |
| `dn7-4` | **The page tells the truth where the eye actually is.** Three findings raised **by eye, by the owner**, on 2026-09-10 — ⛔ by no check in this repository — and ⛔ **none of them a false statement by the page**: all three are **placement** and **legibility**. 🔴 *(1)* A verb's verdict was written **four gestures below** the button that ran it: the transcript had **one** place, after the fourth step, while *Stop the agent* lives at the **first**. That is **the same defect `dn7-2-2` fixed for *Remove the agent***, surviving **the other way round** — a **fixed** place cannot serve two buttons four gestures apart. The transcript is now **moved under the button just clicked**, and returns to the place it holds **in the document** when nobody asked, because a page-level fact written with **no click** must ⛔ never be attributed to the last button pressed. *(2)* The language gesture showed a **guess** where the eye was and the **exact fact** one step lower: the console already named *the picker was closed without selecting the line* in step 4, while step 3 read *most often a port picker is already open*. The named refusal now surfaces in step 3, ⛔ **with no new message and no new key** — the guess **stays**, for the one case where it is **true**, an opening genuinely already in flight. *(3)* The active position of a language picker is now a **filled block** rather than an outline: both state rules carried **exactly** the declarations of the hover rule, so hovering an inactive position made it **look active**. ⛔ **No colour was added.** ⚠️ **What is measured and what is not**: a new check, `tools/verif_placement_dn74.py` (42 checks, 25 mutants), keeps the **mechanism** — its thirteen new checks were played against the **original** page first and all thirteen came back **red** — and the dynamic bench `tools/verif_banc_langue_dn73.py`, which **executes** the page's script, saw **three of its cases turn red** the moment the fix landed, because their expected text still spelled out the defect. ⛔ **No check here runs the page in a browser or draws anything**, so whether the filled position reads as selected at a glance closes **at the owner's eye** — same family as `AC7.2.2.2` — and that observation was ⛔ **not** recorded when this line was published. Following the rule stated for `dn5-4`, the state written is the one the marker has in the commit that publishes this line, and that state is `in review`. ⚠️ **State corrected within the same day, by the review this sentence anticipated: this row read `in review`, and the four-layer review has since run and closed the marker on 2026-09-10.** ⛔ The earlier value is named here rather than erased. It returned **35 findings — 27 patched, 2 carried, 6 rejected with reasons** (four of them **refuted on a measurement**, ⛔ not on an opinion), and its heaviest family is the one this repository has already paid for: **a gate that stayed GREEN on code that was FALSE**, four times, each one **replayed** rather than argued — the checks were reading a **CSS comment**; the fill test accepted the button's own **resting background**; the disabled test never looked at the **border**; an **undeclared** custom property was invisible to every gate. It also found **two product defects**, one of which a check was actively **locking in**: the state refresh chained after a successful verb writes with no anchor, so a verb that succeeds and is then followed by a failing refresh sent the verdict four gestures away again. And the fix had created **its own regression**: the active-and-disabled rule made the selected position identical to the unselected one while the gesture runs — a distinction that **held** before this marker. ⚠️ **Two of the errors were in the story, ⛔ not in the code**, and they are named there. ⚠️ **What is still NOT closed**: whether the filled position and the relocated verdict read correctly **at a glance** is measured **by the owner's eye** on the served page, and that observation has ⛔ not been recorded — it is carried in the planning ledger with a live carrier. Following the same `dn5-4` rule, the state written is the one the marker has in the commit that publishes this line, and that state is `done`. | done |
| `dn7-5` | **The declared gap names LibreHardwareMonitor, it is now re-derived rather than recopied, and the agent refuses to start without it.** Raised on 2026-09-10 **by a question from the owner in session** — ⛔ by no check in this repository, and by no review: *« is LHM a HARD prerequisite, because otherwise the panel is useless »*. Measured the same day: the install page, its local server and this very roadmap carried **zero** mention of LHM while `agent/dn_agent.py` carried 142 on 129 lines, and the only complete sentence about it in the product lived in a `stderr` line printed **when a scheduled task shuts down**. Three moves, ⛔ not one added line: the gap **names** LHM on the three surfaces a newcomer crosses, with what falls without it — the CPU temperature and the three fan speeds, ⛔ nothing else — its **own** gesture (⛔ not `pip`) and a **three-position** state; that gap is now **re-derived** from the agent's real imports **through the AST**, in both directions, so the next missing dependency is impossible to keep quiet; and the one pre-flight in this repository that **really refuses** — the agent's, ⛔ not the installer's — stops on its own exit code. 🔴 **And the price is written, ⛔ not hidden**: a machine without LHM used to keep everything except the four quantities LibreHardwareMonitor publishes — the CPU temperature and the three fan speeds — and now loses the agent entirely, so the README table that measures the opposite is **annotated rather than erased**; and because the LHM task is elevated while the agent's is not, the two race at logon — the existing recovery is 3 restarts, 1 minute apart, and **beyond about three minutes the agent is absent for the whole session**. ⚠️ **What no check here can close**: that a real tower without LHM refuses. That is measured on Windows and recorded under `mesures/dn7-5/`, where the agent's absence is **verified** rather than deduced from the message. Following the same `dn5-4` rule, the state written is the one this marker has in the commit that publishes this line.  ⚠️ **State corrected the same day, by the review this row anticipated: it read `in review`, and the four-layer review has since run and closed the marker on 2026-09-10.** ⛔ The earlier value is named here rather than erased. It returned **40 findings — 17 entries fixed, 3 carried, 4 rejected with reasons** (two of them **refuted on a measurement**). 🔴 **Its heaviest finding came from no check at all, and it made this marker actively harmful**: the gesture published on **four** surfaces installed LibreHardwareMonitor for **that session only** — the script itself states that the logon task needs a second switch, and registers none without it — so a newcomer following the page **to the letter** got an agent that worked once and was refused at **every** logon after, on a machine they had just set up correctly; and the whole published cost analysis rested on a premise that gesture never produced. Three more of the same family as this repository's oldest fault — **a gate staying GREEN on code that was FALSE** — were each **replayed** rather than argued: an English-only regression, an **inverted** probe that would refuse on a healthy tower, and a renamed switch that left five gates green. ⚠️ **And two published numbers were wrong the same way**: *15 of 20 quantities* counted **sensors** and called them quantities — four fall, not five, and the denominator was sourced nowhere. ⛔ **What no check here closes**: that a real tower without LibreHardwareMonitor refuses. That is measured on Windows, and its record is a **protocol that has not been played**; its carrier moved to the epic at closure, because the marker that held it now closes. | done |
| `dn7-6` | **The install page now installs what it can, and it blocks on what it cannot.** Raised on 2026-09-10 **by five requests from the owner in session** — ⛔ by no check in this repository, and by no review. Three arbitrations were settled in that session and are quoted rather than paraphrased: *« it installs as far as possible, otherwise it exposes »*, *« blocking means a greyed-out button when the conditions are not met »*, and *« fine for the new verb that starts the agent »*. The first of them **amends this epic's own thread** — `dn7` used to *expose* and build nothing, which was true from `dn7-1` to `dn7-3` and stops being true here. Measured the same day, before a line was written: the four steps of the workflow carried **no state at all**, the page's only disarmed gesture was disarmed on **one** condition among **seventeen** scattered `.disabled` assignments and ⛔ no common factory, and the two gestures the page **publishes to copy** were plain strings. 🔴 **And the most expensive hole was new**: since `dn7-5` LibreHardwareMonitor is a **hard** prerequisite of the agent, so a newcomer could install the agent and discover **at the next logon, without a line on disk**, that it refuses to start — while the page already **knew** LHM was missing and used that for nothing. Four moves: every step carries a **three-position** state witness, in the vocabulary the panel itself uses, and every one of those identifiers is in the list the page resets when its server goes quiet; a **single** factory greys a gesture **and writes the reason under the button**; the page **runs** the dependency gesture itself, non-elevated, with the very interpreter that serves it and derived from the published string rather than a second copy of it; and a **new verb** activates the agent by **composing** the two halves that existed separately — the scheduled task, whose trigger is the logon alone, and the immediate start, which survives nothing — relaying its own exit codes and reserving one for the half-success where the task is installed and verified but no agent came up. ⛔ **What it does not install, it names**: LibreHardwareMonitor, whose gesture needs administrator rights this installer never asks for, and Python itself, which cannot be a precondition of a page that does not exist without it — both with the reason written beside them. ⚠️ **A declared gap, and it is the heart of the first acceptance criterion**: the state witness is ⛔ **not green**, although the owner asked for green dots. Three measurements decide it, ⛔ no argument: this page's identity is a closed list of seven roles, each **read out of the firmware**, and none of them is green; green was **tried on the product and abandoned**, on the owner's own eye, because it did not read well on a green PCB; and `dn7-4` already settled on this very page that the filled state is a difference of **shape**, ⛔ not of hue. The carrier is named: an owner arbitration that amends the identity file with a **live** firmware source. ⛔ **What no check here closes**: that a button is really greyed under someone's eyes, and that an agent really comes up. That is measured on Windows, and its record is a **protocol that has not been played**. ⚠️ **State corrected the same day, by the review this row anticipated: it read `in review`, and the four-layer review has since run and closed the marker on 2026-09-10.** ⛔ The earlier value is named here rather than erased. It returned **34 findings — 28 fixed, 4 carried, 3 rejected**, ⛔ none refuted. 🔴 **Its heaviest defect came from the verification pass rather than the review, and it was green everywhere**: the witness's CSS comment closed too early, leaving an orphan `*/`, so a browser swallowed the *ready* pill — the very thing the owner asked to see — while three gates stayed green on it. 🔴 **And the verification-gap layer demonstrated four more, each leaving eight gates green**: the URL the page posts to was never confronted with the one the server serves; **no harness ever drove a step into the BLOCKED position**, the state this marker is named after; the two banners' visibility was never read; and the reserved half-success code had no consumer-side test. All six regressions were replayed after the fix, and all six now turn red. 🔴 **Two product defects announced a false success**: an unset exit code yields **0**, so the page said *the tool returned 0* on a gesture that did nothing; and with no board enumerated **Activate stayed armed**, which would register a logon task on the tool's hard-coded port and fail silently every session. Following the same `dn5-4` rule, the state written is the one the marker has in the commit that publishes this line, and that state is `done`. | done |
| `dn5` | The epic these `dn5-*` markers belong to: **the repository clones, builds, and stops talking about one machine**. | in progress |
| `dn5-1` | Publishing the repository, and writing down the gap between what it **asks** of a reader and what it **can do** — [`dn5-1-ecart-promesses.md`](dn5-1-ecart-promesses.md). | done |
| `dn5-2` | Making the repository keep the promises it **already published**: licensing coverage, `THIRD-PARTY.md`, the CLA bot that `CONTRIBUTING.md` mentioned but that did not exist, and the `CHANGELOG.md` claim about NVIDIA GPUs that the agent code refutes. | in progress |
| `dn5-3` | Removing every remaining dependency on the author's own machine, and declaring the ones that only *name* it. | done |
| `dn5-4` | The firmware builds in a **fresh directory**, from a clean clone. ⚠️ **State corrected on 2026-09-04, by that work itself: this row read `not started` while the marker was under way** — the commit carrying this correction is the same one that adds the cold-build measurements and the *Building the firmware* section of [`CONTRIBUTING.md`](../CONTRIBUTING.md). ⚠️ **Corrected again on 2026-09-04 at the code review, and the reason is worth more than the value:** the correction above wrote `in progress` while the plan already read `review`, and this page **distinguishes the two** (see `dn4-42`). A state written for the moment of writing is stale by the time it is pushed; what is written now is the state the marker has **in the commit that publishes this line** — and the review that produced this correction closed the marker in the same gesture, so it lands here as `done`. | done |
| `dn4-44` | **No new check ships without a mutant that replants the fault it guards against** — a rule about the repository's own gates, not about the firmware. It armed the acceptance criterion that `dn4-40` handed on. | done |
| `dn4-45` | **Building the firmware in CI.** Deliberately out of scope for `dn5-4`, which measured what it would cost: a runner would install ESP-IDF v5.5.5 (**3.84 GiB**, 23 submodules) and its toolchains (**4.30 GiB**), then pull **172.5 MiB** of components — against a workflow that today runs on `ubuntu-latest` with a 20-minute timeout and no `pip install` at all. 🔴 And one measured fact decides more than the sizes: **a cold build needs the network, and no local archive cache replaces it** (`dependencies.lock` is gitignored, so a fresh clone must *solve*, and solving queries the registry). | not started |
| `dn5-5` | The clone states its own size, and everything still missing before a public release has a named owner. ⚠️ **State corrected on 2026-09-05, by that work itself: this row read `not started` while the marker was being finished** — the commit carrying this correction is the same one that adds the *What cloning this costs* section of [`CONTRIBUTING.md`](../CONTRIBUTING.md) and the clone measurements under `mesures/dn5-5/`. ⛔ The earlier value is named here rather than erased. Following the rule stated for `dn5-4`, what is written is the state the marker has **in the commit that publishes this line**: its code review has not run yet, so it lands here as `in review`, ⛔ not `done`. ⚠️ **Corrected within the same day, and the sentence above is kept rather than replaced: the review has run.** It raised twenty-six findings, all of them acted on, and two were **demonstrated** rather than argued — the check that was supposed to prove the removal only looked for the *presence* of a re-query, so swapping two branches made the page say the task was gone while it survived, and the exit codes the README publishes as "the mechanical instrument of the gap" were played by nothing at all. Both now run the shipped code against a paper double. 🔴 **The review also paid for its own trap**: after demonstrating a fault and restoring the file, the check kept failing on a sound source, because the ordinary import path reuses `__pycache__`, validated on size and timestamp — a check that measures yesterday's code is worse than no check, and the source is now read and compiled instead. The state stays `in review`: the review produced fixes, it did ⛔ not close the marker. | in review |
| `dn5-6` | Re-checking the findings that earlier reviews deferred **before acting on any of them** — ten of them, replayed one at a time and each classed *still true* / *already false* / *not reproducible*. ⛔ Not a fix-up story: a finding that no longer reproduces is closed with its date and its reason, not corrected just in case. | in review |
| `dn8-1` | **The checks parse what they judge, and their helpers live in one place.** Shipped on 2026-09-11: a shared helper module (`tools/dn_gates.py`) holding the eight names that the checks had been copying, a new check over it (`tools/verif_harnais_dn81.py`), and **one** control added to the ledger check so that the planning tracker is now **parsed** rather than scanned line by line. ⚠️ What it deliberately does **not** do is written on the check itself: the **101 copies / 57 distinct implementations** measured across the existing checks on 2026-09-11 are **inventoried and printed at every run**, ⛔ never counted as failures — reworking them belongs to `dn4`, and a rework done blind is a rework nobody has seen fail. | done |
| `dn8` | The public face: splitting the long French `README.md` into a short front page plus an engineering log, a version **shown on the device**, a release tag, the templates needed to receive issues and pull requests, and **proving the CLA bot against a real outside contributor** — which cannot happen while the repository is private and has no fork. | epic opened, **no stories written yet** |

⚠️ **`dn8` is the least defined entry here, and that is stated rather than dressed
up.** It is an open epic with no stories behind it: when `CONTRIBUTING.md` says a
version displayed on the device is "still to come", `dn8` names *where that work will
live*, not work that is under way.

⚠️ **Amended on 2026-09-11, and the paragraph above is kept rather than replaced (the
rule of this repository is to annotate, ⛔ not to erase).** The sentence *"an open epic
with no stories behind it"* was true when it was written and is **no longer**: `dn8` now
carries **eight** ordered stories, and the first of them shipped on the date of this
line. ⚠️ The order is itself a measured decision, ⛔ not a preference for rigour: the
public front page is built by splitting a file that has grown to **239 678 bytes /
2 345 lines** (re-measured 2026-09-11, against 172 791 on 2026-08-31), and splitting
that much prose into *N* files multiplies by *N* the places where a check that reads
**text** can be green on something false. ⇒ the tooling goes first, and the visible
value arrives **at the end of the epic**. That cost is written here rather than
discovered later.

## What is already known to be wrong, and where it is written

The gap between what this repository **asks of you** and what it **can currently
do** was measured and written down before the code was published. It is in
[`dn5-1-ecart-promesses.md`](dn5-1-ecart-promesses.md) — six entries, each with the
marker that carries it. That page is in French; it is part of the engineering log,
and splitting the two is itself part of `dn8`.
