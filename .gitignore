# ESP-IDF / build
build/
managed_components/
dependencies.lock

# sdkconfig est GÉNÉRÉ (par set-target puis menuconfig) : il ne va pas au dépôt.
# Ce qui fait foi, c'est sdkconfig.defaults, qui lui est versionné.
sdkconfig
sdkconfig.old

# Python
__pycache__/
*.pyc
.venv/

# OS / éditeurs
.DS_Store
Thumbs.db
